# dsh-manager — 设置页管理入口 bundle patch
# 注册为 profile 的一个 bundle layer；浏览器半边在设置页的 nav rail 里
# 加入「MCP / Skill / Agent」分区，点击打开桌面端管理窗口（manager.html）。
- insert:
    - id: dsh-manager
      name: 'dsh-manager'
